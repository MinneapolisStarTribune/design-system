import { Meta, ColorPalette, ColorItem } from '@storybook/blocks';
import { startribuneColorCategories } from '../brandColorsData';

<Meta title="Foundations/Colors/Star Tribune/Semantic Colors" />

{startribuneColorCategories
  .filter((category) => category.name === 'Semantic')
  .map((category) => (
    <div key={category.name}>
      <h1>{category.name}</h1>
      {category.description && <p>{category.description}</p>}
      <ColorPalette>
        {category.colors.map((color) => (
          <ColorItem
            key={color.name}
            title={`${category.name}: ${color.name}`}
            subtitle={color.description}
            colors={{ [color.tokenName]: color.value }}
          />
        ))}
      </ColorPalette>
    </div>
  ))}

