import { Meta, ColorPalette, ColorItem } from '@storybook/addon-docs/blocks';
import { buttonColors } from './colorsData';

<Meta title="Foundations/Colors/Button Colors" />

# Button Colors

Shared button colors that are used across all brands. These include filled, outlined, ghost, and utility button styles.

<ColorPalette>
  {buttonColors.map((color) => (
    <ColorItem
      key={color.tokenName}
      title={color.name}
      subtitle={color.value}
      colors={{ [color.tokenName]: color.value }}
    />
  ))}
</ColorPalette>

