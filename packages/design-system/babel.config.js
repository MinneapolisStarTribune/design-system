// Used exclusively by Jest for React Native test transforms.
// Vite/esbuild handles all production builds — this file is not used outside of Jest.
module.exports = {
  presets: ['@react-native/babel-preset'],
};
