import { Meta } from '@storybook/addon-docs/blocks';
import categories from './component-matrix.json';

<Meta title="Getting Started/Platform Support Matrix" />

# Platform Support Matrix

This matrix shows which components are available on Web, Native, or both.

{categories.map((cat) => (

  <section key={`${cat.topLevel}-${cat.subCategory}`}>
    <h2>{cat.topLevel}</h2>
    {cat.subCategory && <h3>{cat.subCategory}</h3>}

    {cat.groups.map((group) => (
      <section key={`${cat.topLevel}-${cat.subCategory}-${group.name || 'default'}`}>
        {group.name && <h4>{group.name}</h4>}
          <table
            style={{
              width: '100%',
              tableLayout: 'auto',
              borderCollapse: 'collapse',
            }}
          >
            <thead>
              <tr>
                <th style={{ textAlign: 'center', verticalAlign: 'middle', padding: '4px 8px' }}>
                  Component
                </th>
                <th style={{ textAlign: 'center', verticalAlign: 'middle', padding: '4px 8px' }}>
                  Web
                </th>
                <th style={{ textAlign: 'center', verticalAlign: 'middle', padding: '4px 8px' }}>
                  Native
                </th>
              </tr>
            </thead>
            <tbody>
              {group.rows.map((row) => (
                <tr key={row.component}>
                  <td style={{ textAlign: 'center', verticalAlign: 'middle', padding: '4px 8px' }}>
                    {row.component}
                  </td>
                  <td style={{ textAlign: 'center', verticalAlign: 'middle', padding: '4px 8px' }}>
                    {row.web ? '✅' : '❌'}
                  </td>
                  <td style={{ textAlign: 'center', verticalAlign: 'middle', padding: '4px 8px' }}>
                    {row.native ? '✅' : '❌'}
                  </td>
                </tr>
              ))}
            </tbody>
        </table>
      </section>
    ))}

  </section>
))}
