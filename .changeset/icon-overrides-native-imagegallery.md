---
"@minneapolisstartribune/design-system": patch
---

Icon: allow `className`/style overrides to pass through to the rendered icon, and updated `Button` to forward icon overrides correctly. Native `ImageGallery` and `Caption` now accept override props so consumers can customize their native rendering.
