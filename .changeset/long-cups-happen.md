---
'@minneapolisstartribune/design-system': patch
---

Add `zIndex` prop to `Tooltip` to allow consumers to override the default stacking order (`9999`) when the tooltip renders behind fixed headers or other high z-index elements.
