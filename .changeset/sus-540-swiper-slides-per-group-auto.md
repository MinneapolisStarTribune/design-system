---
"@minneapolisstartribune/design-system": minor
---

Add `slidesPerGroup` and `slidesPerGroupAuto` props to `SwiperCarousel`. When used with `slidesPerView="auto"`, navigation advances by the number of currently visible slides instead of one slide at a time.
